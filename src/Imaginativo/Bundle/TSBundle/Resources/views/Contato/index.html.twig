{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Contato list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Nome</th>
                <th>Cargo</th>
                <th>Funcao</th>
                <th>Email</th>
                <th>Telefone</th>
                <th>Celular</th>
                <th>Obs</th>
                <th>Status</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('contato_show', { 'id': entity.id }) }}">{{ entity.nome }}</a></td>
                <td>{{ entity.cargo }}</td>
                <td>{{ entity.funcao }}</td>
                <td>{{ entity.email }}</td>
                <td>{{ entity.telefone }}</td>
                <td>{{ entity.celular }}</td>
                <td>{{ entity.obs }}</td>
                <td>{{ entity.status }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('contato_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('contato_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('contato_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
