{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Endereco list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Logradouro</th>
                <th>Bairro</th>
                <th>Cidade</th>
                <th>Uf</th>
                <th>Cep</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('endereco_show', { 'id': entity.id }) }}">{{ entity.logradouro }}</a></td>
                <td>{{ entity.bairro }}</td>
                <td>{{ entity.cidade }}</td>
                <td>{{ entity.uF }}</td>
                <td>{{ entity.cEP }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('endereco_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('endereco_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('endereco_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
